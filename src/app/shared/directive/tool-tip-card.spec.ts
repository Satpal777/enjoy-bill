import { ToolTipCard } from './tool-tip-card';

describe('ToolTipCard', () => {
  it('should create an instance', () => {
    const directive = new ToolTipCard();
    expect(directive).toBeTruthy();
  });
});
